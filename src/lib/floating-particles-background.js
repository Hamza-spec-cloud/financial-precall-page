// Floating dust particles background with mouse interaction
// Verbatim Framer module source — downloaded from
// https://framerusercontent.com/modules/s7WaD2jg1NpbS1fQ0YiS/yY4SPTplzSd09KMWBopV/FloatingParticlesBackground.js
// (referenced by user as https://framer.com/m/FloatingParticlesBackground-wpRj.js@yY4SPTplzSd09KMWBopV).
// The only edit to the upstream source is replacing the `from "framer"` import with
// inline no-op shims for addPropertyControls / ControlType / useIsStaticRenderer so
// the module runs outside of Framer Studio. Runtime behavior is unchanged.
import{jsx as _jsx}from"react/jsx-runtime";import{useEffect,useRef,useState,useCallback}from"react";
const addPropertyControls=()=>{};
const ControlType=new Proxy({},{get:()=>""});
const useIsStaticRenderer=()=>false;/**
 * @framerSupportedLayoutWidth any
 * @framerSupportedLayoutHeight any
 */export default function FloatingParticlesBackground(props){const{particleCount=50,particleSize=2,particleOpacity=.6,glowIntensity=10,movementSpeed=.5,mouseInfluence=100,backgroundColor="#000000",particleColor="#FFFFFF",mouseGravity="none",gravityStrength=50,glowAnimation="ease",particleInteraction=false,interactionType="bounce",paused=false}=props;const canvasRef=useRef(null);const animationRef=useRef();const mouseRef=useRef({x:0,y:0});const particlesRef=useRef([]);const isStatic=useIsStaticRenderer();const[canvasSize,setCanvasSize]=useState({width:800,height:600});const containerRef=useRef(null);const pausedRef=useRef(paused);pausedRef.current=paused;const initializeParticles=useCallback((width,height)=>{return Array.from({length:particleCount},(_,index)=>({x:Math.random()*width,y:Math.random()*height,vx:(Math.random()-.5)*movementSpeed,vy:(Math.random()-.5)*movementSpeed,size:Math.random()*particleSize+1,opacity:particleOpacity,baseOpacity:particleOpacity,mass:Math.random()*.5+.5,id:index}));},[particleCount,particleSize,particleOpacity,movementSpeed]);const redistributeParticles=useCallback((width,height)=>{particlesRef.current.forEach(particle=>{// Redistribute particles proportionally across the new dimensions
particle.x=Math.random()*width;particle.y=Math.random()*height;});},[]);const updateParticles=useCallback(canvas=>{const rect=canvas.getBoundingClientRect();const mouse=mouseRef.current;particlesRef.current.forEach((particle,index)=>{// Calculate distance to mouse
const dx=mouse.x-particle.x;const dy=mouse.y-particle.y;const distance=Math.sqrt(dx*dx+dy*dy);// Mouse influence and gravity
if(distance<mouseInfluence&&distance>0){const force=(mouseInfluence-distance)/mouseInfluence;const normalizedDx=dx/distance;const normalizedDy=dy/distance;const gravityForce=force*(gravityStrength*.001);// Apply gravity effect based on mouseGravity setting
if(mouseGravity==="attract"){particle.vx+=normalizedDx*gravityForce;particle.vy+=normalizedDy*gravityForce;}else if(mouseGravity==="repel"){particle.vx-=normalizedDx*gravityForce;particle.vy-=normalizedDy*gravityForce;}particle.opacity=Math.min(1,particle.baseOpacity+force*.4);// Apply glow animation based on type
const targetGlow=1+force*2;const currentGlow=particle.glowMultiplier||1;if(glowAnimation==="instant"){particle.glowMultiplier=targetGlow;}else if(glowAnimation==="ease"){// Ease in-out animation
const easeSpeed=.15;particle.glowMultiplier=currentGlow+(targetGlow-currentGlow)*easeSpeed;}else if(glowAnimation==="spring"){// Spring animation with overshoot
const springForce=(targetGlow-currentGlow)*.2;const damping=.85;particle.glowVelocity=(particle.glowVelocity||0)*damping+springForce;particle.glowMultiplier=currentGlow+particle.glowVelocity;}}else{particle.opacity=Math.max(particle.baseOpacity*.3,particle.opacity-.02);// Return glow to normal based on animation type
const targetGlow=1;const currentGlow=particle.glowMultiplier||1;if(glowAnimation==="instant"){particle.glowMultiplier=targetGlow;}else if(glowAnimation==="ease"){const easeSpeed=.08;particle.glowMultiplier=Math.max(1,currentGlow+(targetGlow-currentGlow)*easeSpeed);}else if(glowAnimation==="spring"){const springForce=(targetGlow-currentGlow)*.15;const damping=.9;particle.glowVelocity=(particle.glowVelocity||0)*damping+springForce;particle.glowMultiplier=Math.max(1,currentGlow+particle.glowVelocity);}}// Particle interaction
if(particleInteraction){for(let j=index+1;j<particlesRef.current.length;j++){const other=particlesRef.current[j];const dx=other.x-particle.x;const dy=other.y-particle.y;const distance=Math.sqrt(dx*dx+dy*dy);const minDistance=particle.size+other.size+5;if(distance<minDistance&&distance>0){if(interactionType==="bounce"){// Elastic collision
const normalX=dx/distance;const normalY=dy/distance;// Relative velocity
const relativeVx=particle.vx-other.vx;const relativeVy=particle.vy-other.vy;// Relative velocity in collision normal direction
const speed=relativeVx*normalX+relativeVy*normalY;// Only resolve if velocities are separating
if(speed<0)return;// Collision impulse
const impulse=2*speed/(particle.mass+other.mass);// Update velocities
particle.vx-=impulse*other.mass*normalX;particle.vy-=impulse*other.mass*normalY;other.vx+=impulse*particle.mass*normalX;other.vy+=impulse*particle.mass*normalY;// Separate particles to prevent overlap
const overlap=minDistance-distance;const separationX=normalX*overlap*.5;const separationY=normalY*overlap*.5;particle.x-=separationX;particle.y-=separationY;other.x+=separationX;other.y+=separationY;}else if(interactionType==="merge"){// Temporary merge effect - increase glow and size
const mergeForce=(minDistance-distance)/minDistance;particle.glowMultiplier=(particle.glowMultiplier||1)+mergeForce*.5;other.glowMultiplier=(other.glowMultiplier||1)+mergeForce*.5;// Attract particles slightly
const attractForce=mergeForce*.01;particle.vx+=dx*attractForce;particle.vy+=dy*attractForce;other.vx-=dx*attractForce;other.vy-=dy*attractForce;}}}}// Update position
particle.x+=particle.vx;particle.y+=particle.vy;// Add subtle random movement
particle.vx+=(Math.random()-.5)*.001;particle.vy+=(Math.random()-.5)*.001;// Damping
particle.vx*=.999;particle.vy*=.999;// Boundary wrapping
if(particle.x<0)particle.x=rect.width;if(particle.x>rect.width)particle.x=0;if(particle.y<0)particle.y=rect.height;if(particle.y>rect.height)particle.y=0;});},[mouseInfluence,mouseGravity,gravityStrength,glowAnimation,particleInteraction,interactionType]);const drawParticles=useCallback(ctx=>{ctx.clearRect(0,0,ctx.canvas.width,ctx.canvas.height);particlesRef.current.forEach(particle=>{ctx.save();// Create glow effect with enhanced blur based on interaction
const currentGlowMultiplier=particle.glowMultiplier||1;ctx.shadowColor=particleColor;ctx.shadowBlur=glowIntensity*currentGlowMultiplier*2;ctx.globalAlpha=particle.opacity;ctx.fillStyle=particleColor;ctx.beginPath();ctx.arc(particle.x,particle.y,particle.size,0,Math.PI*2);ctx.fill();ctx.restore();});},[particleColor,glowIntensity]);const animate=useCallback(()=>{const canvas=canvasRef.current;if(!canvas)return;if(pausedRef.current){animationRef.current=undefined;return;}const ctx=canvas.getContext("2d");if(!ctx)return;updateParticles(canvas);drawParticles(ctx);animationRef.current=requestAnimationFrame(animate);},[updateParticles,drawParticles]);const handleMouseMove=useCallback(e=>{const canvas=canvasRef.current;if(!canvas)return;const rect=canvas.getBoundingClientRect();mouseRef.current={x:e.clientX-rect.left,y:e.clientY-rect.top};},[]);const resizeCanvas=useCallback(()=>{const canvas=canvasRef.current;const container=containerRef.current;if(!canvas||!container)return;const rect=container.getBoundingClientRect();const newWidth=rect.width;const newHeight=rect.height;canvas.width=newWidth;canvas.height=newHeight;// Update canvas size state and redistribute particles
setCanvasSize({width:newWidth,height:newHeight});// Only redistribute if particles exist and size changed significantly
if(particlesRef.current.length>0){redistributeParticles(newWidth,newHeight);}},[redistributeParticles]);// Effect to reinitialize particles when particle count changes
useEffect(()=>{if(isStatic)return;const canvas=canvasRef.current;if(!canvas)return;particlesRef.current=initializeParticles(canvas.width||canvasSize.width,canvas.height||canvasSize.height);},[particleCount,initializeParticles,isStatic,canvasSize]);// Effect to update particle properties when they change
useEffect(()=>{if(isStatic)return;particlesRef.current.forEach(particle=>{particle.baseOpacity=particleOpacity;particle.opacity=particleOpacity;// Update velocity based on new movement speed
const currentSpeed=Math.sqrt(particle.vx*particle.vx+particle.vy*particle.vy);if(currentSpeed>0){const ratio=movementSpeed/currentSpeed;particle.vx*=ratio;particle.vy*=ratio;}});},[particleOpacity,movementSpeed,isStatic]);useEffect(()=>{if(isStatic)return;resizeCanvas();if(typeof window!=="undefined"){window.addEventListener("mousemove",handleMouseMove);window.addEventListener("resize",resizeCanvas);}// Set up ResizeObserver for container
if(containerRef.current&&typeof ResizeObserver!=="undefined"){const resizeObserver=new ResizeObserver(()=>{resizeCanvas();});resizeObserver.observe(containerRef.current);return()=>{resizeObserver.disconnect();if(typeof window!=="undefined"){window.removeEventListener("mousemove",handleMouseMove);window.removeEventListener("resize",resizeCanvas);}};}return()=>{if(typeof window!=="undefined"){window.removeEventListener("mousemove",handleMouseMove);window.removeEventListener("resize",resizeCanvas);}};},[handleMouseMove,resizeCanvas,isStatic]);useEffect(()=>{if(isStatic||paused)return;animate();return()=>{if(animationRef.current){cancelAnimationFrame(animationRef.current);}};},[animate,isStatic,paused]);if(isStatic){return /*#__PURE__*/_jsx("div",{style:{width:"100%",height:"100%",backgroundColor,position:"relative",overflow:"hidden"},children:Array.from({length:Math.min(particleCount,20)}).map((_,i)=>/*#__PURE__*/_jsx("div",{style:{position:"absolute",left:`${Math.random()*100}%`,top:`${Math.random()*100}%`,width:`${particleSize}px`,height:`${particleSize}px`,backgroundColor:particleColor,borderRadius:"50%",opacity:particleOpacity,boxShadow:`0 0 ${glowIntensity}px ${particleColor}`}},i))});}return /*#__PURE__*/_jsx("div",{ref:containerRef,style:{width:"100%",height:"100%",backgroundColor,position:"relative",overflow:"hidden"},children:/*#__PURE__*/_jsx("canvas",{ref:canvasRef,style:{width:"100%",height:"100%",display:"block"}})});}addPropertyControls(FloatingParticlesBackground,{particleCount:{type:ControlType.Number,title:"Particle Count",defaultValue:50,min:10,max:1e3,step:1},particleSize:{type:ControlType.Number,title:"Particle Size",defaultValue:2,min:1,max:10,step:.5,unit:"px"},particleOpacity:{type:ControlType.Number,title:"Particle Opacity",defaultValue:.6,min:.1,max:1,step:.1},glowIntensity:{type:ControlType.Number,title:"Glow Intensity",defaultValue:10,min:0,max:30,step:1,unit:"px"},glowAnimation:{type:ControlType.Enum,title:"Glow Animation",options:["instant","ease","spring"],optionTitles:["Instant","Ease In-Out","Spring"],defaultValue:"ease",displaySegmentedControl:true},movementSpeed:{type:ControlType.Number,title:"Movement Speed",defaultValue:.5,min:.1,max:2,step:.1},mouseInfluence:{type:ControlType.Number,title:"Mouse Influence",defaultValue:100,min:50,max:300,step:10,unit:"px"},backgroundColor:{type:ControlType.Color,title:"Background Color",defaultValue:"#000000"},particleColor:{type:ControlType.Color,title:"Particle Color",defaultValue:"#FFFFFF"},mouseGravity:{type:ControlType.Enum,title:"Mouse Gravity",options:["none","attract","repel"],optionTitles:["None","Attract","Repel"],defaultValue:"none",displaySegmentedControl:true},gravityStrength:{type:ControlType.Number,title:"Gravity Strength",defaultValue:50,min:10,max:200,step:10,hidden:({mouseGravity})=>mouseGravity==="none"},particleInteraction:{type:ControlType.Boolean,title:"Particle Interaction",defaultValue:false,enabledTitle:"On",disabledTitle:"Off"},interactionType:{type:ControlType.Enum,title:"Interaction Type",options:["bounce","merge"],optionTitles:["Bounce","Merge"],defaultValue:"bounce",displaySegmentedControl:true,hidden:({particleInteraction})=>!particleInteraction}});
export const __FramerMetadata__ = {"exports":{"default":{"type":"reactComponent","name":"FloatingParticlesBackground","slots":[],"annotations":{"framerSupportedLayoutHeight":"any","framerContractVersion":"1","framerSupportedLayoutWidth":"any"}},"__FramerMetadata__":{"type":"variable"}}}
//# sourceMappingURL=./FloatingParticlesBackground.map